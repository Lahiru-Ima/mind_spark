import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xFF00B2B2);
const kSecondaryColor = Color(0xFFCFCFCF);
const kTextColor = Color(0xFF2B2B2B);
const kWhiteColor = Colors.white;
const kExtraColor = Color(0xFF00A3FF);
final kShadowColor =  const Color(0xFFD3D3D3).withOpacity(.84);

const kDefaultPadding = EdgeInsets.symmetric(horizontal: 20);
const kAnimationDuration = Duration(milliseconds: 200);
